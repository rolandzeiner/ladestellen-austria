# Minimum-version floors so a transitive minor release of any of these
# can't break green-on-main without a commit on our side. Bump when the
# project intentionally adopts new behaviour from a newer release.
# Local dev tracks the floor or higher; CI installs the floor's minor
# resolved by pip.
pytest-homeassistant-custom-component>=0.13.205,<0.14
pytest-asyncio>=0.24.0,<1
pytest-cov>=6.0.0,<7
syrupy>=4.8.0,<5
mypy>=1.20.0,<2
ruff>=0.15.0,<1

# Transitive pin — pycares 5.x removed `ares_query_a_result`, which
# aiodns 3.5.0 still references in its type hints. aiodns 3.6+ fixed it,
# but Home Assistant 2025.1.x pins aiodns < 3.6, so the only stable
# combination on the current HA pin is aiodns 3.5.x + pycares 4.x.
# Bump pycares cap together with aiodns when HA ships >= 3.6.
pycares<5
