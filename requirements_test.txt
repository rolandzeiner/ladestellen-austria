# Minimum-version floors so a transitive minor release of any of these
# can't break green-on-main without a commit on our side. Bump when the
# project intentionally adopts new behaviour from a newer release.
# Local dev tracks the floor or higher; CI installs the floor's minor
# resolved by pip.
pytest-homeassistant-custom-component>=0.13.205,<0.14
pytest-asyncio>=1.3.0,<2
pytest-cov>=6.0.0,<7
syrupy>=4.8.0,<5
mypy>=1.20.0,<2
ruff>=0.15.0,<1

# Transitive pin — aiodns 3.5.0 references `pycares.ares_query_a_result`
# which was removed/renamed in pycares 5.x. Until aiodns ships a
# pycares-5-compatible release, hold the transitive resolution at 4.x.
# Drop this line once `import aiodns` no longer crashes on pycares 5.
pycares<5
